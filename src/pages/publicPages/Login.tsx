import React, { FC } from 'react'
import FormLogin from '../../components/Forms/FormLogin'

const Login: FC = () => {
    return (
        <main>
            <FormLogin />
        </main>
    )
}

export default Login