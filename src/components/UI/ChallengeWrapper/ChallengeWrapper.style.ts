import styled from "@emotion/styled";

export const ChallengesWrapper = styled("ul")({
    boxShadow: "0 0 10px #362666",
    background: "#362666",
    color: "#e7dfff",
    borderRadius: 10,
    padding: 20,
    marginBottom: 25,
});